import 'package:flutter/material.dart';

class MyEnvironmentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('My environment'),
      ),
    );
  }
}
